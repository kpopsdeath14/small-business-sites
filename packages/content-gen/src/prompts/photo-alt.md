Для сайта бизнеса "{{name}}" ({{business_type}}) сгенерируй короткие alt-тексты для фотографий (для SEO и доступности), по одному предложению на фото, на русском, без слова "фото" в начале.

Список фото (JSON, значения — url, порядок важен): {{photos}}

Ответь ТОЛЬКО валидным JSON без пояснений, строго такой формы:
{"alts": ["...", "..."]}

Массив alts должен быть той же длины и в том же порядке, что и список фото.
