---
import Container from "@sitegen/shared-ui/components/Container.astro";
import BrassRule from "../components/BrassRule.astro";
import type { SiteData } from "@sitegen/shared-ui/types.ts";

interface Props {
  data: SiteData;
}
const { data } = Astro.props;

function groupByCategory(items: SiteData["items"]) {
  const map = new Map<string, SiteData["items"]>();
  for (const item of items) {
    const cat = item.category || "Услуги";
    if (!map.has(cat)) map.set(cat, []);
    map.get(cat)!.push(item);
  }
  return Array.from(map.entries());
}
const groups = groupByCategory(data.items);
---

<section id="services" data-reveal class="relative bg-[var(--color-surface)] py-20 sm:py-28">
  <Container narrow>
    <div class="text-center mb-14">
      <BrassRule class="mx-auto mb-5 max-w-[100px]" />
      <h2 class="font-[var(--font-heading)] uppercase text-[clamp(2rem,5vw,3.5rem)] leading-none text-[var(--color-text)]">
        Прайс-лист
      </h2>
    </div>
    <div class="grid sm:grid-cols-2 gap-x-12 gap-y-10">
      {groups.map(([category, items]) => (
        <div>
          <p class="text-xs uppercase tracking-[0.3em] text-[var(--color-primary)] font-semibold mb-4">{category}</p>
          <ul class="space-y-3">
            {items.map((item) => (
              <li class="flex items-baseline gap-3">
                <span class="text-[var(--color-text)]">{item.name}</span>
                <span class="flex-1 border-b border-dotted border-[var(--color-border)] translate-y-[-3px]"></span>
                {item.price && <span class="font-[var(--font-heading)] text-[var(--color-primary)] whitespace-nowrap">{item.price}</span>}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </Container>
</section>
