---
import Container from "@sitegen/shared-ui/components/Container.astro";
import Lightbox from "@sitegen/shared-ui/components/Lightbox.tsx";
import BrassRule from "../components/BrassRule.astro";
import type { SiteData } from "@sitegen/shared-ui/types.ts";

interface Props {
  data: SiteData;
}
const { data } = Astro.props;
---

<section id="gallery" data-reveal class="relative bg-[var(--color-bg)] py-16 sm:py-24">
  <Container class="mb-10">
    <BrassRule class="mb-5 max-w-[100px]" />
    <h2 class="font-[var(--font-heading)] uppercase text-[clamp(2rem,5vw,3.5rem)] leading-none text-[var(--color-text)]">
      Наши работы
    </h2>
  </Container>

  <Lightbox
    client:visible
    photos={data.gallery}
    gridClassName="columns-2 sm:columns-3 lg:columns-4 gap-1 px-1"
    itemClassName="mb-1 break-inside-avoid"
    imgClassName="w-full h-auto"
  />
</section>
