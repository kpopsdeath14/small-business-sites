---
import Container from "@sitegen/shared-ui/components/Container.astro";
import Section from "@sitegen/shared-ui/components/Section.astro";
import Card from "@sitegen/shared-ui/components/Card.astro";
import type { SiteData } from "@sitegen/shared-ui/types.ts";

interface Props {
  data: SiteData;
}
const { data } = Astro.props;

function groupByCategory(items: SiteData["items"]) {
  const map = new Map<string, SiteData["items"]>();
  for (const item of items) {
    const cat = item.category || "Услуги";
    if (!map.has(cat)) map.set(cat, []);
    map.get(cat)!.push(item);
  }
  return Array.from(map.entries());
}
const groups = groupByCategory(data.items);
---

<Section id="services">
  <Container narrow>
    <h2 class="font-[var(--font-heading)] text-3xl font-bold text-[var(--color-text)] text-center mb-10">Услуги и цены</h2>
    <Card hover={false} class="space-y-10">
      {groups.map(([category, items]) => (
        <div>
          <h3 class="text-sm uppercase tracking-wide text-[var(--color-primary)] font-semibold mb-4">{category}</h3>
          <ul class="space-y-3">
            {items.map((item) => (
              <li class="flex items-baseline gap-2">
                <span class="text-[var(--color-text)]">{item.name}</span>
                <span class="flex-1 border-b border-dotted border-[var(--color-border)] translate-y-[-2px]"></span>
                {item.price && <span class="font-medium text-[var(--color-text)] whitespace-nowrap">{item.price}</span>}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </Card>
  </Container>
</Section>
