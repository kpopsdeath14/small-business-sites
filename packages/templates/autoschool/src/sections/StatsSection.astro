---
import Container from "@sitegen/shared-ui/components/Container.astro";
import Section from "@sitegen/shared-ui/components/Section.astro";
import type { SiteData } from "@sitegen/shared-ui/types.ts";

interface Props {
  data: SiteData;
}
const { data } = Astro.props;
---

<Section id="stats" spacing="md" class="bg-[var(--color-primary)]">
  <Container>
    <div class="grid grid-cols-2 sm:grid-cols-4 gap-6">
      {data.stats.map((stat) => (
        <div class="text-center">
          <p class="font-[var(--font-heading)] uppercase text-3xl sm:text-4xl text-[var(--color-primary-contrast)]">{stat.value}</p>
          <p class="mt-1 text-xs uppercase tracking-widest text-[var(--color-primary-contrast)]">{stat.label}</p>
        </div>
      ))}
    </div>
  </Container>
</Section>
