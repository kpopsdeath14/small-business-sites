---
import type { SiteData } from "@sitegen/shared-ui/types.ts";
import GalleryGrid from "./Gallery/GalleryGrid.astro";
import GalleryMarquee from "./Gallery/GalleryMarquee.astro";

interface Props {
  variant: number;
  data: SiteData;
}
const { variant, data } = Astro.props;
const variants = [GalleryGrid, GalleryMarquee];
const Variant = variants[variant] ?? variants[0];
---

<Variant data={data} />
