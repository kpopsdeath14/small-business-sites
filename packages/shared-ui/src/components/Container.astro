---
interface Props {
  class?: string;
  narrow?: boolean;
}
const { class: className = "", narrow = false } = Astro.props;
---

<div class:list={["mx-auto px-4 sm:px-6 lg:px-8", narrow ? "max-w-3xl" : "max-w-6xl", className]}>
  <slot />
</div>
